name: Build

on:
  push:
    paths:
      - ".github/workflows/build.yml"
      - "build.yaml"
      - "config/**"
  pull_request:
    paths:
      - ".github/workflows/build.yml"
      - "build.yaml"
      - "config/**"
  workflow_dispatch:

jobs:
  build:
    uses: uruz-7/zmk/.github/workflows/build-user-config.yml@v0.3.5
